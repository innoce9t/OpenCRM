# Local config with your API key — never commit this.
config.php

# Stored API connections (contains live keys) — never commit this.
/data/

# Composer (only if you add dependencies later)
/vendor/
composer.lock

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/
